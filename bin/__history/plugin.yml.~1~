name: BASeCamp Survival Chests
main: com.BASeCamp.SurvivalChests.BCRandomizer
version: 1.6
commands:
      repopchests:
        description: repopulates chests in loaded chunks.
      preparegame:
        description: prepares a game to start, and announces the game to players as well as those that join.
      joingame:
        description: used to join a game being prepared.
      spectategame:
        description: used to spectate a game being prepared, or a game in progress.
      startgame:
        description: starts a game being prepared.
      friendly:
        description: shows the friendly name for the held item.
      randomizespawners:
        description: randomizes the creature type and NBT Data for spawners.
      gamestate:
        description: shows state of running games.
      setlives:
        description: sets number of times a player will respawn during a game. set to 0 to make it continuous. Respawned players start with a shuffled inventory.
      concludegame:
        description: Sets all active games to conclude.
      mobmode:
        description: toggles "mobmode" on and off.
      arenaborder1:
        description: set's first border corner.
      arenaborder2:
        description: set's second border corner.
      clearborders:
        description: clears any set borders.
      saveborder:
        description: saves the currently set border for the current world.
      borders:
        description: displays the currently set borders.
      loadborder:
        description: loads the currently saved border for the current world.
      repoptimeout:
        description: sets or views the current chest repopulation timeout, in seconds.
      mobtimeout:
        description: sets the timeout after a PvP match in which the mob arena spawn logic will kick in.
      mobsweeper:
        description: enables the Mob Arena spawn logic for a PvP match in progress.
      newarena:
        description: attempts to generate a new arena, and teleports the user to it.
      listworlds:
        description: lists all current worlds.
      mwtp:
        description: teleports you to the spawn location of another world.
      mwdel:
        description: deletes a world.
      autoprepare:
        description: turns autopreparation on or off.
      joinstartdelay:
        description: sets the delay after enough people have joined a game at which time it will automatically start. use 0 to disable auto-start after join.
      hardcorehealth:
        description: enables/disables hardcore health mode. hardcore health mode prevents regeneration from hunger.